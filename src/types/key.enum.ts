export enum GO {
  Forward,
  Left,
  Right,
  Reverse,
}

export enum GO_FORWARD {
  ArrowUp = 10,
}

export enum GO_LEFT {
  ArrowLeft = 40,
}

export enum GO_RIGHT {
  ArrowRight = 20,
}

export enum GO_REVERSE {
  ArrowDown = 30,
}
